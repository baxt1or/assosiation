import LoadingSpinner from "@/components/shared/LaodingSpinner";
import { Loader2 } from "lucide-react";
import React from "react";

const Loading = () => {
  return <LoadingSpinner />;
};

export default Loading;
